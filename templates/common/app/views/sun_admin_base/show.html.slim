= page_header t(action_name) + t(obj_str)
- obj = single_obj
.am-margin
  table class='#{table_class}'
    - obj.class.used_attrs.each do |a|
      tr
        td = localize_attr(a)
        td = obj.send(a) 
  = link_to_back
